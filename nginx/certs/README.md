Place the production TLS certificate chain and private key here as:

- `fullchain.pem`
- `privkey.pem`

The PEM files are ignored by Git. Restrict access to the private key to the
deployment account. For local testing, generate a temporary self-signed
certificate using the command documented in the project README.
